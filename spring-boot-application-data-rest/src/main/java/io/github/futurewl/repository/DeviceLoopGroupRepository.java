package io.github.futurewl.repository;

import io.github.futurewl.entity.DeviceLoopGroup;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DeviceLoopGroupRepository extends JpaRepository<DeviceLoopGroup, Long> {
}
